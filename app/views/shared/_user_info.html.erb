<%= link_to gravatar_for(current_user, size: Settings.avatar_size),
                        current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to (t "v_sh_ui.view_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count,
                   (t "v_sh_ui.micropost")) %></span>
