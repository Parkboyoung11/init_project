<%= form_for current_user.active_relationships.build, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @entry.user.id %></div>
  <%= f.submit (t "v_u_fo.follow"), class: "btn btn-primary" %>
<% end %>
